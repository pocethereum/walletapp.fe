import { CoinDisplayPipe } from './coin-display.pipe';

describe('CoinDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new CoinDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
