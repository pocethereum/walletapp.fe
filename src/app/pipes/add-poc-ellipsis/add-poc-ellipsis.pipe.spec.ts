import { AddPocEllipsisPipe } from './add-poc-ellipsis.pipe';

describe('AddPocEllipsisPipe', () => {
  it('create an instance', () => {
    const pipe = new AddPocEllipsisPipe();
    expect(pipe).toBeTruthy();
  });
});
